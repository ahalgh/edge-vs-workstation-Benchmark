# Core dependencies (install PyTorch separately first — see setup.py or README)
# PyTorch install is platform-specific:
#   Windows/Linux: pip install torch --index-url https://download.pytorch.org/whl/cu124
#   Jetson Thor:   use JetPack pre-installed torch or NVIDIA's aarch64 wheel
numpy>=1.24.0,<2.0.0
pynvml>=11.5.0
pyyaml>=6.0
matplotlib>=3.7.0
seaborn>=0.13.0
tqdm>=4.65.0
psutil>=5.9.0
